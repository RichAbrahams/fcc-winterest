// import { fromJS } from 'immutable';
// import { makeSelectMyWinsDomain } from '../selectors';

// const selector = makeSelectMyWinsDomain();

describe('makeSelectMyWinsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
