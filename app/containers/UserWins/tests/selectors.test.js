// import { fromJS } from 'immutable';
// import { makeSelectUserWinsDomain } from '../selectors';

// const selector = makeSelectUserWinsDomain();

describe('makeSelectUserWinsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
